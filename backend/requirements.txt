# --- Core API ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- Data ---
SQLAlchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3          # PostgreSQL driver (Neon / Cloud SQL)

# --- Validation / settings ---
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0

# --- Security ---
bcrypt==4.2.1
PyJWT==2.10.1

# --- Media & documents ---
Pillow==11.0.0
reportlab==4.2.5

# --- Optional: Google Cloud Storage (prod image storage) ---
# Install only when STORAGE_BACKEND=gcs
google-cloud-storage==2.19.0
