# Frontend environment. Copy to .env.local for local overrides.
#
# Leave VITE_API_URL unset in development: Vite proxies /api to the backend
# on http://127.0.0.1:8000 (see vite.config.ts).
#
# In production, point it at your deployed backend:
# VITE_API_URL=https://gym-api-xxxxx-uc.a.run.app/api
